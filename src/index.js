const add = require('./lib/add')

const centuryFromYear = require('./lib/centuryFromYear')

const checkPalindrome = require('./lib/checkPalindrome')

const isLucky = require('./lib/isLucky')

const isIPv4Address = require('./lib/isIPv4Address')

module.exports = {
  add,
  centuryFromYear,
  checkPalindrome,
  isLucky,
  isIPv4Address
}
